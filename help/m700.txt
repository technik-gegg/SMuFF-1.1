M700	 [S | P [length]]
---------------------------
S 		load filament to Splitter
P 		purge filament only (if no length is given, purge the configured length)

Without parameters filament of the current tool gets loaded down to the nozzle (default for non Splitter option).